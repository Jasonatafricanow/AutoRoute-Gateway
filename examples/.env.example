# model-gateway secrets (§14 三分离: secret) — copy to .env, never commit.
# This file holds real keys; gateway.db / logs must never contain them.

AMD_API_KEY=
MODELSCOPE_API_KEY=
OPENCODE_KEY_1=
OPENCODE_KEY_2=
GEMINI_API_KEY=

# startup settings
# GATEWAY_BIND=127.0.0.1
# GATEWAY_PORT=8700
# DATABASE_PATH=gateway.db
# GATEWAY_AUTH_TOKEN=
